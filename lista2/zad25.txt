SELECT K.IMIE, K.FUNKCJA, K.PRZYDZIAL_MYSZY
FROM KOCURY K NATURAL JOIN BANDY
WHERE K.PRZYDZIAL_MYSZY+1 > ALL (
    SELECT PRZYDZIAL_MYSZY*3 
    FROM KOCURY K1 NATURAL JOIN BANDY B 
    WHERE K1.FUNKCJA='MILUSIA' AND (B.TEREN='SAD' OR B.TEREN='CALOSC')
);